<%= form_with(model: log, local: true) do |form| %>
  <% if log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(log.errors.count, "error") %> prohibited this log from being saved:</h2>

      <ul>
      <% log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :log_date %>
    <%= form.date_select :log_date %>
  </div>

  <div class="field">
    <%= form.label :log_time %>
    <%= form.time_select :log_time %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_area :location %>
  </div>

  <div class="field">
    <%= form.label :participant %>
    <%= form.text_area :participant %>
  </div>

  <div class="field">
    <%= form.label :sensor_type %>
    <%= form.text_area :sensor_type %>
  </div>

  <div class="field">
    <%= form.label :reading %>
    <%= form.text_field :reading %>
  </div>

  <div class="field">
    <%= form.label :observation %>
    <%= form.text_area :observation %>
  </div>

  <div class="field">
    <%= form.label :img %>
    <%= form.text_area :img %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
